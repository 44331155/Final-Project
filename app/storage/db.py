# 先占位：后续可接入 SQLAlchemy/SQLite
# from sqlalchemy import create_engine
# engine = create_engine("sqlite:///./app.db", future=True, echo=False)
# SessionLocal = sessionmaker(bind=engine, autocommit=False, autoflush=False)
pass